{
  "name": "cursorlearning",
  "version": "1.0.0",
  "description": "Cursor Training + лендинг интенсива",
  "type": "module",
  "scripts": {
    "build": "mkdir -p .vercel-output && cp -r cursor-intensive-landing/* .vercel-output/ && cp -r api .vercel-output/"
  }
}
