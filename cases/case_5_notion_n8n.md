# Кейс 5: Связка с Notion и n8n

## Ситуация
Каждый понедельник ты тратишь 1.5 часа на рутину: выгрузить метрики, заполнить таблицу в Notion, написать саммари, отправить в Telegram. Хочешь автоматизировать это через n8n — и сделать это прямо из Cursor, без ручной сборки воркфлоу.

## Данные
- `data/n8n_notion_context.md` — описание текущей боли, структура Notion, желаемый результат

---

## Часть 1: Подключаем n8n MCP к Cursor

MCP (Model Context Protocol) — это способ дать Cursor прямой доступ к внешним сервисам. С n8n MCP Cursor сможет **сам создавать и деплоить воркфлоу** в твоём n8n, без копирования JSON.

### Шаг 1. Убедись что Node.js установлен

Открой терминал в Cursor (`` Ctrl+` ``) и проверь:
```
node --version
```
Если не установлен — скачай с nodejs.org.

### Шаг 2. Получи API-ключ в n8n

1. Открой свой n8n (облачный n8n.cloud или локальный)
2. Перейди в **Settings → API**
3. Нажми **Create API Key**
4. Скопируй ключ и URL своего n8n (например `https://your-name.app.n8n.cloud`)

### Шаг 3. Создай конфиг MCP в проекте

В корне проекта создай папку `.cursor` и файл `mcp.json`:

```
📁 cursorlearning/
  📁 .cursor/
    📄 mcp.json
```

Содержимое `mcp.json`:

```json
{
  "mcpServers": {
    "n8n-mcp": {
      "command": "npx",
      "args": ["n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "N8N_API_URL": "https://your-name.app.n8n.cloud",
        "N8N_API_KEY": "твой-api-ключ-из-n8n",
        "LOG_LEVEL": "error",
        "DISABLE_CONSOLE_OUTPUT": "true"
      }
    }
  }
}
```

> **Замени** `N8N_API_URL` и `N8N_API_KEY` на свои реальные значения.

### Шаг 4. Включи MCP в Cursor

1. Открой **Cursor Settings** (`Cmd+,` / `Ctrl+,`)
2. В поиске набери **MCP**
3. Убедись что n8n-mcp отображается и включён (зелёный индикатор)
4. Если не появился — перезапусти Cursor

### Шаг 5. Проверь подключение

Открой чат Cursor (`Cmd+L`) и напиши:
```
Покажи список моих воркфлоу в n8n
```
Если всё настроено — Cursor покажет список через MCP. Если нет — проверь API-ключ и URL.

---

## Часть 2: Создаём воркфлоу через Cursor

Теперь Cursor может напрямую создавать воркфлоу в n8n. Не нужно копировать JSON — он сделает всё сам.

### Промпт для Cursor

```
Прочитай data/n8n_notion_context.md — описание задачи по автоматизации еженедельного отчёта.

Создай n8n-воркфлоу, который:
1. Запускается по расписанию каждый понедельник в 9:00
2. Делает HTTP-запрос к Analytics API (GET /analytics/weekly-summary)
3. Парсит JSON с метриками
4. Добавляет строку в Notion-базу «Weekly Metrics» (колонки: Week, MAU, DAU, D1 Retention, D7 Retention, Avg Check, NPS, Points Earned, Points Redeemed)
5. Генерирует текстовое саммари через AI-ноду (на основе метрик и highlights)
6. Обновляет Notion-страницу «Weekly Update» саммари
7. Отправляет саммари в Telegram-канал

Используй плейсхолдеры для токенов, я заменю потом:
- Analytics API: Bearer токен
- Notion: internal integration token
- Telegram: bot token + chat id
```

---

## Часть 3 (опционально): Без MCP — ручной импорт

Если MCP не подключён, Cursor может сгенерировать JSON для ручного импорта:

```
Прочитай data/n8n_notion_context.md.

Сделай:
1. JSON-файл n8n_workflow.json — готовый к импорту воркфлоу для n8n
2. Пошаговый гайд n8n_setup_guide.md — как импортировать и настроить

Ноды: Schedule Trigger → HTTP Request → Function → Notion (create row) → AI → Notion (update page) → Telegram
```

Затем в n8n: **Workflows → Import from File → выбери n8n_workflow.json**.

---

## Что демонстрирует
- **MCP** — Cursor управляет внешними сервисами напрямую, без копипасты
- Cursor проектирует автоматизации и создаёт их в n8n за минуты
- AI — не только про код, но и про рабочие процессы
- Экономит 6+ часов в месяц на рутинной отчётности

## Время: 5-7 минут (с настройкой MCP), 3-4 минуты (только генерация)

## Важно для демо
Этот кейс — показательный. Он демонстрирует не «напиши код», а «автоматизируй мою работу». Cursor через MCP становится пультом управления для внешних сервисов — это то, что цепляет менеджеров больше всего.
